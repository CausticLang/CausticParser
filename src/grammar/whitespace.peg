NEWLINE <- '\r'? '\n' { auxil->loc.line++; auxil->loc.line_at = $0e; }

_ws <- ( [ \t]+ / NEWLINE+ )+

_ct <- '//' [^\n] NEWLINE
     / '/*' (NEWLINE / [^*] / ('*' [^/]))* '*/' ~ { _cap_ERROR("_ct", "Expected end of block comment"); }
_ <- (_ws / _ct)*

EOF <- !.
